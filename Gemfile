# 同步github pages所需组件
# 项目地址：https://github.com/github/pages-gem
# 安装ruby后（包括MSYS2），运行
# gem update bundler
# bundle install
# 即可
source 'https://rubygems.org'
gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-last-modified-at"
end
